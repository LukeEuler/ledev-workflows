# {{项目名}}

> 用于生成面向开发者、新人、组内同伴和技术负责人阅读的项目文档。正文以中文为主；路径、命令、符号、配置项、状态值可以保留英文。
>
> 写作要求：
> - 只写来自代码、配置、事实层、QA 或人工确认的内容。
> - 重要结论必须附关键证据，例如路径、符号、命令、配置项或 QA 编号。
> - 所有路径必须可移植：主仓库路径相对项目根目录，关联仓库路径使用相对主仓库路径或 `related:<repo>:<path>`；不要写本机绝对路径。
> - 不确定内容写 `未确认：...`；已检查但没有发现证据写 `未发现明确证据：...` 并说明检查范围。
> - 图示使用纯 ASCII 字符，不使用 Mermaid、图片或外部依赖。
> - 表格中无法确认的单元格不要留空，写 `未确认`、`不适用` 或 `未发现明确证据：...`。
> - 明确区分业务链路、运行时依赖、工程支撑和代码边界；不要把 `go.mod`、`Makefile`、CI、lint、测试、dev scripts、generated、third-party 当成业务上下游或业务外部依赖。

## 1. 项目定位

用一段话说明项目解决什么问题、面向什么业务或工程场景、主要产出是什么。

- 一句话定位：
- 主要使用者/调用方：
- 主要产出：
- 关键证据：

## 2. 项目背景与系统边界

### 2.1 业务背景

说明项目所在的业务背景、触发场景、上游需求或历史原因。

- 背景说明：
- 业务触发源：
- 已确认依据：
- 未确认问题：

### 2.2 本项目负责什么

| 负责范围 | 说明 | 入口/模块 | 证据 |
| --- | --- | --- | --- |
|  |  |  |  |

### 2.3 本项目不负责什么

| 不负责范围 | 说明 | 交由谁/哪里处理 | 证据 |
| --- | --- | --- | --- |
|  |  |  |  |

### 2.4 多仓库上下文

如果当前项目是单仓库，写 `不适用：单仓库上下文`。如果存在关联仓库，必须区分主仓库、关联仓库、扫描深度、写入边界和版本关系。不要把关联仓库的本地当前 checkout 自动写成主仓库实际依赖代码，除非解析证据支持。

| 仓库 | 角色 | 本地路径 | 扫描深度 | 写入策略 | 声明版本 | 实际解析来源 | 本地 checkout | 版本关系 | 证据 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
|  | primary/related |  | deep/shallow/metadata-only | writable/read-only |  |  |  | match/mismatch/not-applicable/unknown |  |

- 跨仓边界：
- 版本不一致风险：
- 需要人工确认：

## 3. 架构总览

说明项目在系统中的定位、上下游关系和核心运行模式。ASCII 图只画对理解业务链路重要的对象：上游业务输入、核心执行单元、关键输出或下游系统。除非对业务理解很关键，基础设施中间件不要画进主图。工程支撑内容不要画进主架构图，例如 `go.mod`、`Makefile`、CI、lint、测试命令、dev scripts、generated/third-party 边界应放到“项目约定”“代码导航索引”或“外部依赖与集成”的工程支撑表。

```text
[上游/调用方]
      |
      v
[核心入口/适配层] -> [核心执行单元] -> [输出/下游]
                           |
                           v
                    [关键数据/副作用]
```

- 系统定位：
- 上游关系：
- 下游关系：
- 核心运行模式：
- 不进入主图的工程支撑：
- 关键证据：

## 4. 核心抽象与心智模型

解释项目中最重要的抽象：它代表什么业务概念、如何被创建、如何推进或被消费、为什么对理解项目重要。只列影响业务理解和运行链路的抽象；普通工具类、配置 helper、构建脚本、测试夹具、生成代码、third-party 包不要列为核心抽象，除非它们直接承载业务语义。

| 抽象 | 业务含义 | 如何推动业务流程 | 创建者 | 推进/消费 | 为什么重要 | 关键代码证据 |
| --- | --- | --- | --- | --- | --- | --- |
|  |  |  |  |  |  |  |

补充说明：

- 最重要的心智模型：
- 容易误解的地方：
- 未确认问题：

## 5. 业务能力与模块划分

业务能力优先来自 `.ai/project-context.md`、事实层、长期 QA 或人工确认。每个能力都要能追溯到代码或配置。只列项目对业务或运行链路提供的能力，不要把构建、lint、测试、发版、代码生成、CI、dev scripts 和目录约定列为业务能力。

| 业务能力 | 解决的问题 | 关键业务输入 | 关键业务输出 | 关键代码/类 | 业务/运行时依赖 | 关键证据 |
| --- | --- | --- | --- | --- | --- | --- |
|  |  |  |  |  |  |  |

### 5.1 工程支撑能力

工程支撑能力只记录开发、构建、测试、生成、发布和本地辅助能力，不和业务能力并列。

| 工程支撑能力 | 作用 | 入口/命令/文件 | 影响范围 | 证据 |
| --- | --- | --- | --- | --- |
| 构建/测试/格式化 |  |  |  |  |
| 代码生成/third-party 边界 |  |  |  |  |
| 本地开发辅助 |  |  |  |  |

## 6. 代码/模块映射

| 模块 | 对应业务能力 | 关键点/特殊点 | 入口/主要符号 | 证据 |
| --- | --- | --- | --- | --- |
|  |  |  |  |  |

## 7. 新人快速上手

### 7.1 模块速览

| 模块/目录 | 含义 | 什么时候需要读 | 推荐先看 | 注意事项 |
| --- | --- | --- | --- | --- |
|  |  |  |  |  |

### 7.2 入口怎么找

| 入口类型 | 推荐入口 | 用途 | 证据 |
| --- | --- | --- | --- |
| 程序启动入口 |  |  |  |
| 核心流程入口 |  |  |  |
| 配置入口 |  |  |  |
| API/命令入口 |  |  |  |
| 测试入口 |  |  |  |

### 7.3 开发从哪里入手

- 修改业务逻辑优先看：
- 修改配置/集成优先看：
- 查运行问题优先看：
- 查依赖问题优先看：
- 修改测试优先看：
- 不建议一开始修改：

### 7.4 阅读建议

1. 先读：
2. 再读：
3. 最后读：
4. 不建议先读：

### 7.5 HTML 快速上手摘要

本节为 HTML 的“新人快速上手”提供结构化来源。只写帮助新人建立心智模型、找到入口、定位问题的稳定信息；不要写完整代码导航索引、内部方法清单或临时排障记录。

| 上手任务 | 推荐入口 | 适用场景 | 注意事项 |
| --- | --- | --- | --- |
| 理解系统做什么 |  |  |  |
| 找核心流程 |  |  |  |
| 改配置/集成 |  |  |  |
| 查运行问题 |  |  |  |
| 写或改测试 |  |  |  |
| 不建议先读 |  |  |  |

## 8. 核心数据与状态模型

本节为 HTML 的“核心数据模型与状态机”提供结构化来源。优先写业务域、核心对象、对象关系、状态枚举和状态流转；不要写字段级大表、完整 DDL、包路径或方法签名。状态必须尽量保留中文名、枚举名和数值，便于 HTML 渲染为 `中文名(ENUM_NAME=数值)`。

### 8.1 数据域划分

| 数据域 | 业务含义 | 包含对象 | 主要创建/更新方 | 主要消费方 | 证据 |
| --- | --- | --- | --- | --- | --- |
|  |  |  |  |  |  |

### 8.2 核心数据对象

| 对象 | 所属数据域 | 业务含义 | 关键字段 | 创建/更新位置 | 消费位置 | 证据 |
| --- | --- | --- | --- | --- | --- | --- |
|  |  |  |  |  |  |  |

### 8.3 数据关系

| 源对象 | 关系 | 目标对象 | 关系含义 | 证据 |
| --- | --- | --- | --- | --- |
|  |  |  |  |  |

### 8.4 状态机

如果没有明确状态机，写 `未发现：状态机`，并说明已检查的事实范围。不要为了凑图编造状态。

| 状态中文名 | 枚举名 | 数值 | 业务含义 | 进入条件 | 退出条件 | 失败/异常去向 | 证据 |
| --- | --- | --- | --- | --- | --- | --- | --- |
|  |  |  |  |  |  |  |  |

### 8.5 状态流转

| 起始状态 | 触发动作 | 目标状态 | 触发方 | 失败处理 | 证据 |
| --- | --- | --- | --- | --- | --- |
|  |  |  |  |  |  |

## 9. 关键业务流程

本节按业务流程逐条写。每条流程必须独立成节，优先描述外部可观察的业务行为，不写内部类名、方法签名或包路径。HTML 会把每条流程渲染成独立标题、业务意图、时序步骤和阶段说明表。

### 9.1 {{流程名}}

- 业务意图：
- 外部触发源：
- 本系统参与身份：
- 直接下游：
- 成功结果：
- 失败结果：
- 关键业务规则：
- 关键证据：

#### 参与方边界

参与方只写三类：外部触发源、本系统整体、直接外部下游。内部类、Controller、Service、Task、Cache、Util、Manager、DB、配置中心、监控系统等不要作为 HTML 时序图参与方；相关读写放到“阶段拆解”的“做什么”里。

| 参与方 | 类型 | 是否本系统内部 | HTML 中如何展示 | 证据 |
| --- | --- | --- | --- | --- |
|  | external-trigger / system / downstream / business-middleware | yes/no |  |  |

#### 时序步骤

```text
[外部触发源] -> [本系统]：触发业务流程
[本系统] -> [本系统]：执行业务校验和核心处理
[本系统] -> [直接下游]：提交请求或输出结果
[直接下游] -> [本系统]：返回处理结果
```

#### 阶段拆解

| 阶段 | 做什么 | 为什么需要 | 失败处理 | 关键代码/配置证据 |
| --- | --- | --- | --- | --- |
|  |  |  |  |  |

## 10. 安全防控

本节为 HTML 的“安全相关”提供结构化来源。优先描述业务安全链路、加密/签名/脱敏场景和安全关注点；不要在 HTML 来源列中展开算法参数明细、具体凭据变量名、密钥值、完整标准对照或审计打分。

### 10.1 业务安全

| 防控点 | 防什么 | 怎么防 | 失败结果 | 证据 | 后续确认 |
| --- | --- | --- | --- | --- | --- |
| 身份认证 |  |  |  |  |  |
| 权限控制 |  |  |  |  |  |
| 输入校验 |  |  |  |  |  |
| 敏感数据 |  |  |  |  |  |
| 外部调用 |  |  |  |  |  |
| 日志与审计 |  |  |  |  |  |

### 10.2 加密相关

| 场景 | 保护对象 | 加密/签名/脱敏手段 | 密钥/凭据托管位置（概念级） | 失败结果 | 证据 | 后续确认 |
| --- | --- | --- | --- | --- | --- | --- |
|  |  |  |  |  |  |  |

### 10.3 安全关注点

| 关注点 | 影响 | 触发条件 | 建议处理/待确认 | 证据 |
| --- | --- | --- | --- | --- |
|  |  |  |  |  |

### 10.4 不进入 HTML 的安全细节

- 具体算法参数：
- 具体凭据变量名：
- 标准对照或审计细节：

## 11. 配置与控制

本节为 HTML 的“配置项”提供结构化来源。HTML 只展示安全相关开关或关闭后会影响认证、授权、加密、外部调用安全、审计、数据保护的控制项；纯业务调参类配置留在 Markdown，不进入 HTML。

### 11.1 安全相关配置项

| 配置项/控制项 | 作用 | 默认值/来源 | 影响范围 | 安全影响 | 关闭/错误配置后果 | 证据 |
| --- | --- | --- | --- | --- | --- | --- |
|  |  |  |  |  |  |  |

### 11.2 不进入 HTML 的配置项

| 配置项/控制项 | 不进入 HTML 的原因 | 影响范围 | 证据 |
| --- | --- | --- | --- |
|  | 纯业务调参/阈值/批大小/展示配置/未确认 |  |  |

- 配置加载路径：
- 环境变量：
- Feature flag/开关：
- 本地运行注意事项：

## 12. 外部依赖与集成

### 12.1 上下游和服务依赖（业务上下游与运行时依赖）

只记录程序运行时真实交互、影响业务结果或数据一致性的依赖。不要写 `go.mod`、`Makefile`、CI、lint、测试、dev scripts、generated 或 third-party 目录边界。

本节为 HTML 的“上下游和服务依赖”提供结构化来源。HTML 会把这些信息渲染为 `flowchart TB` 链路图，箭头标注方向、协议和认证方式，并渲染外部系统清单表。

#### 链路图数据

| 源 | 方向/关系 | 目标 | 协议/方式 | 认证方式 | 失败处理 | 证据 |
| --- | --- | --- | --- | --- | --- | --- |
|  | 调用/读取/写入/发布/订阅/回调 |  | HTTP/RPC/MQ/DB/File/CLI/未确认 | mTLS/JWT/签名/无/未确认 |  |  |

#### 外部系统清单

| 依赖/系统 | 分类 | 方向 | 为什么需要 | 协议/方式 | 认证方式 | 失败影响 | 失败处理 | 证据 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
|  | upstream/downstream/runtime/data-store/internal-business | upstream/downstream/third-party/internal |  |  |  |  |  |  |

### 12.2 工程支撑依赖

记录构建、测试、格式化、代码生成、CI、本地开发和工具链依赖。它们支撑开发交付，不等同于业务外部依赖。

| 支撑项 | 类型 | 作用 | 入口/文件 | 是否运行时必需 | 证据 |
| --- | --- | --- | --- | --- | --- |
| go.mod/module | 包管理/版本 |  |  | no/yes/未确认 |  |
| Makefile/脚本 | 构建命令 |  |  | no/yes/未确认 |  |
| CI/lint/test | 验证工具 |  |  | no/yes/未确认 |  |
| dev/example | 本地开发辅助 |  |  | no/yes/未确认 |  |

### 12.3 代码边界依赖

记录 generated、vendor、third-party、legacy 或不要修改区域。它们是维护边界，不应被写成业务上下游。

| 边界 | 类型 | 处理原则 | 影响范围 | 证据 |
| --- | --- | --- | --- | --- |
|  | generated/vendor/third-party/legacy |  |  |  |

- 关键集成假设：
- 本地开发是否需要外部依赖：
- 未确认依赖：

### 12.4 多仓库依赖解析

本节只在存在 Related repos 或 workspace 时填写；否则写 `不适用：未发现关联仓库`。

| 主仓库依赖 | 关联仓库 | 声明版本 | 实际解析来源 | 本地 checkout | 是否用于本次事实分析 | 风险/说明 | 证据 |
| --- | --- | --- | --- | --- | --- | --- | --- |
|  |  |  | module-cache/vendor/replace/go.work/workspace/local-path/remote/未确认 |  | yes/no/partial |  |  |

## 13. 失败与恢复机制

本节为 HTML 的“故障与恢复”提供结构化来源。失败场景描述应聚焦系统设计的容错机制，不写具体实现缺陷、Bug、待修复项或代码问题。

### 13.1 失败场景

| 失败场景 | 触发条件 | 当前处理 | 恢复方式 | 观测信号 | 失败影响 | 证据 |
| --- | --- | --- | --- | --- | --- | --- |
|  |  |  |  |  |  |  |

### 13.2 恢复设计原则

| 原则 | 适用场景 | 具体做法 | 证据 |
| --- | --- | --- | --- |
| 重试 |  |  |  |
| 超时 |  |  |  |
| 幂等/一致性 |  |  |  |
| 补偿 |  |  |  |
| 人工介入 |  |  |  |

- 重试策略：
- 超时策略：
- 幂等/一致性：
- 补偿机制：
- 人工介入点：

## 14. 多场景/多实现的差异矩阵

用于记录不同运行模式、不同业务场景、不同实现版本、不同部署环境或不同集成方式之间的差异。

本节为 HTML 的“项目特化”提供结构化来源。HTML 会渲染为差异矩阵表；如果项目没有多场景、多实现、特殊部署或迁移差异，写 `不适用：...` 并说明原因，不要留空。

| 场景/实现 | 适用条件 | 入口 | 关键差异 | 依赖 | 风险 | 证据 |
| --- | --- | --- | --- | --- | --- | --- |
|  |  |  |  |  |  |  |

### 14.1 项目特化补充

| 特化项 | 说明 | 影响范围 | 证据/来源 |
| --- | --- | --- | --- |
|  |  |  |  |

## 15. 代码导航索引

| 想了解什么 | 推荐入口 | 相关模块/文件 | 说明 | 证据 |
| --- | --- | --- | --- | --- |
| 程序如何启动 |  |  |  |  |
| 核心业务怎么跑 |  |  |  |  |
| 数据对象在哪里定义 |  |  |  |  |
| 配置在哪里加载 |  |  |  |  |
| 业务/运行时依赖在哪里接入 |  |  |  |  |
| 工程支撑入口在哪里 |  |  |  |  |
| 测试从哪里看 |  |  |  |  |

## 16. 项目约定

本节为 HTML 的“项目约定”提供结构化来源。HTML 会渲染为 `<ul>`，每条约定使用“加粗短语 + 一句话说明”；不要在 HTML 中展开完整代码导航索引、内部方法清单或大段路径列表。

| 约定类型 | 约定内容 | 适用范围 | 证据 |
| --- | --- | --- | --- |
| 目录结构 |  |  |  |
| 命名 |  |  |  |
| 错误处理 |  |  |  |
| 日志 |  |  |  |
| 测试 |  |  |  |
| 生成代码 |  |  |  |
| 不要修改区域 |  |  |  |

### 16.1 HTML 项目约定摘要

| 约定短语 | 一句话说明 | 适用范围 | 证据 |
| --- | --- | --- | --- |
| 目录边界 |  |  |  |
| 验证命令 |  |  |  |
| 不要修改 |  |  |  |

## 17. 其他

### 17.1 人工补充

保留人工补充信息。刷新文档时不要删除，除非用户明确要求。

- 

### 17.2 纠正记录

记录对 AI 或历史文档错误理解的纠正。刷新文档时必须保留。

- 

### 17.3 待确认问题

| 编号 | 问题 | 影响 | 来源 |
| --- | --- | --- | --- |
|  |  |  |  |

### 17.4 参考资料

| 资料 | 用途 | 路径/链接 |
| --- | --- | --- |
|  |  |  |
