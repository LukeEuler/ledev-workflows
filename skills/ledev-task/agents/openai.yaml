interface:
  display_name: "ledev-task"
  short_description: "Typed task workflow for code changes and bug fixes."
  default_prompt: "Use ledev-task to show task stats by default. For new tasks, create or update the task draft, present the final execution summary, and wait for a later user confirmation before implementation unless the request meets the narrow fast-path conditions. Continue, restart, block, or close only when the user explicitly selects that operation."
